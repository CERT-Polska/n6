# Command and Control
Category `cnc` (Command and Control) refers to hosts used as Command and Control servers. All communication from infected machines are going to those servers, including steal authentication data or acknowledgement for remote code execution from infected computer. 
